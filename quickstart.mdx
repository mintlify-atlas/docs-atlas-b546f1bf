---
title: 'Quickstart'
description: 'Get started with VaultSaaS SDK in under 10 minutes'
---

Get your first payment running in under 10 minutes with the VaultSaaS SDK.

## Prerequisites

Before you begin, make sure you have:

- Node.js 20+ (or Bun 1.1+) installed
- A Stripe test secret key (`sk_test_...`)

<Warning>
Always use test API keys during development. Never commit API keys to version control.
</Warning>

## Installation

<Steps>
  <Step title="Install the SDK">
    Install the `@vaultsaas/core` package using your preferred package manager:

    <CodeGroup>
    ```bash npm
    npm install @vaultsaas/core
    ```

    ```bash bun
    bun add @vaultsaas/core
    ```

    ```bash yarn
    yarn add @vaultsaas/core
    ```

    ```bash pnpm
    pnpm add @vaultsaas/core
    ```
    </CodeGroup>
  </Step>

  <Step title="Create your quickstart file">
    Create a new file called `quickstart.ts` in your project directory:

    ```ts quickstart.ts
    import { StripeAdapter, VaultClient, VaultError } from '@vaultsaas/core';

    function mustEnv(name: string): string {
      const value = process.env[name];
      if (!value) {
        throw new Error(`Missing required env var: ${name}`);
      }
      return value;
    }

    const vault = new VaultClient({
      providers: {
        stripe: {
          adapter: StripeAdapter,
          config: {
            apiKey: mustEnv('STRIPE_API_KEY'),
          },
        },
      },
      routing: {
        rules: [{ match: { default: true }, provider: 'stripe' }],
      },
    });

    try {
      const result = await vault.charge({
        amount: 2500,
        currency: 'USD',
        paymentMethod: {
          type: 'card',
          number: '4242424242424242',
          expMonth: 12,
          expYear: 2030,
          cvc: '123',
        },
        customer: {
          email: 'buyer@example.com',
          name: 'Taylor Buyer',
          address: {
            line1: '510 Townsend St',
            city: 'San Francisco',
            postalCode: '94103',
            country: 'US',
          },
        },
        metadata: {
          orderId: 'ord_demo_1001',
          source: 'quickstart',
        },
        idempotencyKey: 'charge-ord_demo_1001-v1',
      });

      console.log('Payment created:', {
        id: result.id,
        status: result.status,
        provider: result.provider,
      });
    } catch (error) {
      if (error instanceof VaultError) {
        console.error('VaultError', {
          code: error.code,
          category: error.category,
          suggestion: error.suggestion,
          docsUrl: error.docsUrl,
          context: error.context,
        });
        process.exit(1);
      }

      throw error;
    }
    ```

    <Note>
    This example uses Stripe's test card number `4242424242424242` which always succeeds. See [Stripe's testing guide](https://stripe.com/docs/testing) for more test cards.
    </Note>
  </Step>

  <Step title="Run the quickstart">
    Execute the file with your Stripe test API key:

    <CodeGroup>
    ```bash bun
    STRIPE_API_KEY=sk_test_xxx bun quickstart.ts
    ```

    ```bash node
    STRIPE_API_KEY=sk_test_xxx node --loader ts-node/esm quickstart.ts
    ```

    ```bash tsx
    STRIPE_API_KEY=sk_test_xxx tsx quickstart.ts
    ```
    </CodeGroup>

    You should see output similar to:

    ```json
    {
      "id": "ch_3AbC123...",
      "status": "succeeded",
      "provider": "stripe"
    }
    ```
  </Step>
</Steps>

## Understanding the code

Let's break down what's happening in the quickstart:

1. **Initialize VaultClient**: Configure the SDK with your payment provider (Stripe) and routing rules
2. **Create a charge**: Call `vault.charge()` with payment details, customer information, and metadata
3. **Handle errors**: Catch and handle `VaultError` instances with detailed error information

<Note>
The `idempotencyKey` ensures that if you run the same charge request multiple times, it won't create duplicate payments. Learn more in the [Idempotency guide](/concepts/idempotency).
</Note>

## Next steps

Now that you've created your first payment, explore these concepts to build production-ready integrations:

<CardGroup cols={2}>
  <Card
    title="Routing"
    icon="route"
    href="/concepts/routing"
  >
    Learn how to intelligently route payments across multiple providers
  </Card>
  <Card
    title="Idempotency"
    icon="shield-check"
    href="/concepts/idempotency"
  >
    Prevent duplicate charges and ensure safe retries
  </Card>
  <Card
    title="Error Handling"
    icon="triangle-exclamation"
    href="/concepts/error-handling"
  >
    Handle payment errors gracefully with detailed error context
  </Card>
  <Card
    title="Webhooks"
    icon="webhook"
    href="/concepts/webhooks"
  >
    Process real-time payment events from providers
  </Card>
</CardGroup>
